/**
 * User account status — active/blocked/pending for user records.
 * For provider application states use ApplicationStatus enum instead.
 */
export enum Status {
  ACTIVE = "active",
  BLOCKED = "blocked",
  PENDING = "pending",
}
