
  // Lifecycle states for a provider account.

export enum ApplicationStatus {
  NOT_APPLIED = "not_applied",
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
  SUSPENDED = "suspended",
}
